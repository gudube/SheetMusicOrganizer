<UserControl x:Class="MusicPlayerForDrummers.View.LibraryMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicPlayerForDrummers.View"
             mc:Ignorable="d" 
             d:DesignHeight="1520" d:DesignWidth="400">
    <Grid Style="{StaticResource BackgroundLv2}">
        <Grid.Resources>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Expander x:Name="PlaylistsExpander" Grid.Row="0" Header="Playlists" IsExpanded="True"> <!-- PLAYLISTS -->
            <local:LibraryListBox/>
        </Expander>
        <Grid Grid.Row="1"/>
    </Grid>
</UserControl>
