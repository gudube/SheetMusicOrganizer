<UserControl x:Class="MusicPlayerForDrummers.View.Controls.Library.MasteryListBox"
             x:Name="MasteryUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:model="clr-namespace:MusicPlayerForDrummers.Model"
             xmlns:local="clr-namespace:MusicPlayerForDrummers.View"
             xmlns:dd="urn:gong-wpf-dragdrop"
             xmlns:items="clr-namespace:MusicPlayerForDrummers.Model.Items"
             xmlns:items1="clr-namespace:MusicPlayerForDrummers.View.Controls.Items"
             mc:Ignorable="d" 
             d:DesignHeight="1520" d:DesignWidth="400"
             Style="{StaticResource InsideLightControl}">
    <ListBox Name="MainListBox" SelectionMode="Multiple" ItemsSource="{Binding Path=Session.MasteryLevels, Mode=OneWay}"
             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
             dd:DragDrop.IsDropTarget="True" dd:DragDrop.DropHandler="{Binding ElementName=MasteryUC}">
        <ListBox.Resources>
            <DataTemplate DataType="{x:Type items:MasteryItem}">
                <items1:SelectableTextItem IsLocked="{Binding IsLocked}" Text="{Binding Name}" IsCustomColor="True" TextColor="{Binding Color}"/>
            </DataTemplate>
        </ListBox.Resources>
    </ListBox>
</UserControl>
