<UserControl x:Class="MusicPlayerForDrummers.View.LibraryView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicPlayerForDrummers.View"
      xmlns:vm="clr-namespace:MusicPlayerForDrummers.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="855" d:DesignWidth="1520">
    <Grid x:Name="MainGrid" Style="{StaticResource BackgroundLv1}">
        <Grid.DataContext>
            <vm:LibraryVM/>
        </Grid.DataContext>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <local:LibraryMenu Grid.Column="0" HorizontalAlignment="Stretch"/>
        <GridSplitter Grid.Column="1" Style="{StaticResource VerticalGridSplitter}"/>
        <DataGrid Grid.Column="2">
            <TextBlock FontSize="12">TEMP</TextBlock>
            <TextBlock>TEMP</TextBlock>
        </DataGrid>
    </Grid>
</UserControl>
